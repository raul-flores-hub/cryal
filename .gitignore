# Run outputs — regenerated by every search, far too large to version
AL_results/
log.txt
*.log
log.lammps

# LAMMPS scratch
structure.data
minimized.data
lammps.out
*.dump
*.restart

# Machine-learned potential files (hundreds of MB; obtain from upstream)
*.model
*.pt
*.model-mliap_lammps.pt

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# The molecule under study is the user's own input, not part of the tool.
# The repository ships only examples/benzene.xyz as a runnable example.
molecule.xyz
